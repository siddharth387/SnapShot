version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm start
      - npm run build
  
  post_build:
    commands:
      - echo "Updating files in the S3 bucket..."
      - aws s3 sync dist/SnapShot/ s3://snapshot.in --delete
